# Quickstart app

Create an iOS 15+ SwiftUI app, add this repository with Swift Package Manager, and replace the generated app file with `QuickstartApp.swift`. Add `applinks:YOUR_APP_KEY.links.wts.is` to Associated Domains and replace the sample public app key. The SDK returns a path; production applications must map it through an explicit route allowlist.
